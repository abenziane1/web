import { StaticPage, staticPageMetadata } from "@/components/layout/StaticPage";

export const metadata = staticPageMetadata("metodologia");

export default function Page() {
  return <StaticPage slug="metodologia" />;
}
