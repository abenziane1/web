import { StaticPage, staticPageMetadata } from "@/components/layout/StaticPage";

export const metadata = staticPageMetadata("privacidad");

export default function Page() {
  return <StaticPage slug="privacidad" />;
}
