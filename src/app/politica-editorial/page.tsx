import { StaticPage, staticPageMetadata } from "@/components/layout/StaticPage";

export const metadata = staticPageMetadata("politica-editorial");

export default function Page() {
  return <StaticPage slug="politica-editorial" />;
}
